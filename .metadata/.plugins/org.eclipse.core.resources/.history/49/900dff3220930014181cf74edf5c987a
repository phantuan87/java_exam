package exam.dev.servlet;

import java.io.IOException;
import java.io.PrintWriter;
import java.text.SimpleDateFormat;
import java.util.Calendar;
import java.util.Locale;
import java.util.TimeZone;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class DailyMood extends HttpServlet {

	private static final long serialVersionUID = -1623573968810638828L;

	@Override
	public void init() throws ServletException {
		// TODO log here
		super.init();
	}
	
	@Override
	protected void doGet(HttpServletRequest req, HttpServletResponse resp)
			throws ServletException, IOException {
		
		
		String hour = getActualTime(req);
		
		PrintWriter out = resp.getWriter();
	    out.println("formatter: " + hour);
	}
	
	private String getActualTime(HttpServletRequest req) {
		Locale locale = req.getLocale();
		Calendar calendar = Calendar.getInstance(locale);
//		TimeZone timeZone = calendar.getTimeZone();
		
		SimpleDateFormat formatter = new SimpleDateFormat("HH");
		
		return formatter.format(calendar.getTime());
	}
}
